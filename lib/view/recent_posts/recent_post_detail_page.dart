import 'package:flutter/material.dart';

class RecentPostDetailPage extends StatelessWidget {
  const RecentPostDetailPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

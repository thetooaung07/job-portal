import 'package:flutter/material.dart';

class PopularJobDetailPage extends StatelessWidget {
  const PopularJobDetailPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
